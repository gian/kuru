val a = (10 : int) * 5

type foo = bool * string * string * (bool * string) option

type k = int * int * program list ref
val bar = x 10 : int
val bar = ref [] : int * int * program list ref

type a = b * c * d * e

type f = (b * c) * (d * (e * f))